/*
import { inject, injectable } from "inversify";
import { TYPES } from "../../types";

@injectable()
export class nHentaiDoujinBookmarkManager{
    private DB : IDBConnection;
    constructor(@inject(TYPES.Mysql_Object) connection : IDBConnection){
        this.DB = connection;
    }
    public createBookmarkList(id : string, bookmark_name : string){

    }
    public addToBookmark(id : string, bookmark_name : string, code : string, doujin_title : string){

    }
    public removeToBookmark(id : string, bookmark_name : string, ind : number){

    }
    public purgeBookmark(id : string, bookmark_name : string){

    }
}
*/